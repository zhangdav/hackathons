import { Signer } from "@ethersproject/abstract-signer";
import { Contract, ContractReceipt } from "@ethersproject/contracts";
import { Network } from "./types";
export declare class SemaphoreContract {
    private contract;
    constructor(contract: Contract);
    createGroup(groupName: string, merkleTreeDepth: number, admin: string): Promise<ContractReceipt>;
    updateGroupAdmin(groupName: string, newAdmin: string): Promise<ContractReceipt>;
    addMember(groupName: string, member: string): Promise<ContractReceipt>;
    addMembers(groupName: string, members: string[]): Promise<ContractReceipt>;
}
export default function getSemaphoreContract(network: Network, privateKeyOrSigner?: string | Signer, apiKey?: string): SemaphoreContract;
